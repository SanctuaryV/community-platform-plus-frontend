# Example environment variables for frontend
# Set REACT_APP_API_URL to the backend base URL (include protocol and port if needed)
# If empty, the app will use relative paths (compatible with CRA proxy)
REACT_APP_API_URL=http://localhost:5000
